level4

1—
#include <iostream>
using namespace std;
int cem(int n) {
    int c = 0;
    for (int i = 2; i <= n; i += 2)
        c += i * i;
    return c;
}
int main() {
    int a;
    cin >> a;
    cout << cem(a);
}
